version:3		# Version increase will cause the package to be re-downloaded
type:texture	# Identifies the type of content
state:optional	# Does the game need this to run (required) or not (optional)
lowend:no 		# Should the bundle be downloaded and used on low end devices?		
